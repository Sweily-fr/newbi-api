type EmailSettings {
  id: ID!
  workspaceId: ID!
  fromEmail: String!
  fromName: String
  replyTo: String
  invoiceEmailTemplate: String
  quoteEmailTemplate: String
  creditNoteEmailTemplate: String
  purchaseOrderEmailTemplate: String
  useCustomFooter: Boolean
  customEmailFooter: String
  verified: Boolean!
  verifiedAt: String
  createdAt: String!
  updatedAt: String!
}

input EmailSettingsInput {
  fromEmail: String!
  fromName: String
  replyTo: String
  invoiceEmailTemplate: String
  quoteEmailTemplate: String
  creditNoteEmailTemplate: String
  purchaseOrderEmailTemplate: String
  useCustomFooter: Boolean
  customEmailFooter: String
}

extend type Query {
  getEmailSettings(workspaceId: ID): EmailSettings
}

extend type Mutation {
  updateEmailSettings(workspaceId: ID, input: EmailSettingsInput!): EmailSettings
}
